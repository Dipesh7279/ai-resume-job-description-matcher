# AI Resume ↔ Job Description Matcher

Compares a resume against a job description using TF-IDF and cosine similarity, giving a match score and highlighting missing keywords.

## Run locally
pip install -r requirements.txt
streamlit run app.py
